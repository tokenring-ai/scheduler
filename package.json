{
  "name": "@tokenring-ai/scheduler",
  "description": "Cron-like service for triggering agent actions at specific intervals.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/chat": "0.2.0",
    "@tokenring-ai/rpc": "0.2.0",
    "@tokenring-ai/utility": "0.2.0",
    "zod": "^4.3.6",
    "moment-timezone": "^0.6.1"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  }
}
