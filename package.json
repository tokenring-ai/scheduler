{
  "name": "@tokenring-ai/scheduler",
  "description": "Service for scheduling AI agents to run at specified intervals",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/chat": "0.2.0",
    "@tokenring-ai/rpc": "0.2.0",
    "@tokenring-ai/utility": "0.2.0",
    "zod": "^4.3.6",
    "moment-timezone": "^0.6.0"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.0.18",
    "typescript": "^5.9.3"
  }
}
